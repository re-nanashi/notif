**Service -> Service (inter-module)**: (entityName) + Response
**Service -> Service (intra-module)**: (entityName) + Dto

**Service -> Controller**: (entityName) + Response
**Service <-> Controller (not an entity)** : AuthenticationResult, CookiePayload, AuthenticationContext (descriptive naming scheme)